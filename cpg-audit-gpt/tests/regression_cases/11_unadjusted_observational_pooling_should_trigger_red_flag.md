# Case: Unadjusted Observational Pooling Should Trigger Red Flag

Input: A guideline pools crude observational event counts or crude OR/RR and
uses the result as a comparative or causal treatment effect.

Expected: Major red flag for confounding not adequately handled unless adjusted
effect estimates, adjustment strategy, adjustment-set differences, and
non-randomized-study RoB are traceable.
